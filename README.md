# เข้าโฟลเดอร์ whatsapp-server
cd whatsapp-server
# เตรียม git repository
git init
git add .
git commit -m "Initial WhatsApp server"
# เชื่อมต่อกับ GitHub (แก้ YOUR_USERNAME)
git remote add origin https://github.com/YOUR_USERNAME/whatsapp-server-2.git
# Push ขึ้น GitHub
git branch -M main
git push -u origin main
